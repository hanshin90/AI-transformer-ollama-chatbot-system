# ========================================================
# HR 규정 기반 RAG 질의 (기본)
# ========================================================
# 문제 요약

# 연차/휴가 관련 질문
# HR 규정만 검색
# bullet 형식으로 답변

result = agent.invoke({
    "messages": [
        ("user",
         "연차 휴가 규정 알려줘. "
         "인사 규정 문서만 보고, 항목 형태로 정리해줘")
    ]
})

print(result["messages"][-1].content)